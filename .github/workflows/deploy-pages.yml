name: Deploy Quarto Site (GitHub Actions Pages)

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Miniconda
        uses: conda-incubator/setup-miniconda@v2
        with:
          auto-update-conda: true
          python-version: 3.11

      - name: Install Quarto CLI (local, no sudo)
        run: |
          set -euo pipefail
          if command -v quarto >/dev/null 2>&1; then
            echo "Quarto already installed: $(quarto --version)"
            exit 0
          fi
          # Try apt quickly (may not be available)
          if sudo apt-get update && sudo apt-get install -y quarto; then
            echo "Installed Quarto via apt"
            quarto --version
            exit 0
          fi
          URL="https://github.com/quarto-dev/quarto-cli/releases/download/v1.8.24/quarto-1.8.24-linux-amd64.tar.gz"
          echo "Downloading Quarto CLI from $URL"
          for i in 1 2 3; do
            curl -fsSL -o quarto.tar.gz "$URL" && break || (echo "download failed, retrying ($i)" && sleep 2)
          done
          mkdir -p quarto-temp
          tar -xzf quarto.tar.gz -C quarto-temp
          QUARTO_DIR=$(echo quarto-temp/* | awk '{print $1}')
          echo "Using extracted Quarto at: $QUARTO_DIR"
          echo "$PWD/$QUARTO_DIR/bin" >> $GITHUB_PATH
          export PATH="$PWD/$QUARTO_DIR/bin:$PATH"
          echo "Quarto path: $(which quarto || echo '$QUARTO_DIR/bin/quarto')"
          "$PWD/$QUARTO_DIR/bin/quarto" --version

      - name: Render Quarto site
        run: |
          quarto render

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./_site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1

